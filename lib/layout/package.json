{
	"name": "@btakita/layout",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "npm run clean && npm run compile",
		"clean": "rm -rf .rpt2_cache; find ./*/index.* -type f -not -name 'index.ts' -delete",
		"compile": "tsc -b",
		"prepublishOnly": "npm run build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"@btakita/web-lib": "workspace:^0.0.1",
		"@ctx-core/fontawesome": "^13.0.22",
		"@ctx-core/nav": "^16.0.25",
		"@ctx-core/sapper": "^11.0.37",
		"@ctx-core/sass": "^10.0.16",
		"@ctx-core/theme": "^4.0.22",
		"svelte": "^3.38.2"
	},
	"devDependencies": {
		"typescript": "^4.3.2"
	}
}